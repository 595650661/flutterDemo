import 'package:flutter/material.dart';
class AirplayVC extends StatelessWidget {
  const AirplayVC({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AirplayVC'),
      ),
      body: Center(child: Text('AirplayVC'),),
    );
  }
}